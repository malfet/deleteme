name: Print Hello
on:
  pull_request:
  push:
    branches:
      - main
      - foobar
    tags:
      - f*

jobs:
  print_hello:
    name: "Print Hello"
    runs-on: ubuntu-20.04
    env:
      EVENT: ${{ toJSON(github) }}
      LIMIT: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref || github.sha }}-${{ github.event_name == 'workflow_dispatch' }}
    steps:
      - name: Print Hello
        run: echo "Hello World"

      - name: Print on Push to foobar
        if: ${{ github.event_name == 'push' && github.event.ref == 'refs/heads/foobar' }}
        run: echo "Hello on push ${{ github.event.ref }}"


